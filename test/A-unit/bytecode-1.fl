program {
    int x;
    goto BYTE;
OK:
    writeln("OK");
    goto END;
ERROR:
    writeln("ERROR");
    goto END;
BYTE:
    bytecode {
        x, 2, ASSIGN,
        x, 2, EQ, OK, JIT,
        ERROR, JMP
    }
END:
    writeln(x);
}