def greet(string name): string {
    return "Привет, " + name + "!";
}

program {
    string me, greeting;
    writeln("Как тебя зовут?");
    read(me);
    greeting = greet(me);
    writeln(greeting);
}