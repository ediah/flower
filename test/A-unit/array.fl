program {
    int[10] x, y, z;

    for (int i = 0; i < 10; i = i + 1) {
        x[i] = i;
        y[i] = i + 1;
        z[i] = x[i] + y[i];
    }

    write("[");
    for (int i = 0; i < 9; i = i + 1)
        write(z[i], ", ");
    write(z[9], "]");
}